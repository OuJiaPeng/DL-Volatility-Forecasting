"""v2 custom model: HAR-prior + residual transformer with a quantile head.

torch is an optional dependency (the ``[model]`` extra). Import submodules explicitly
(``from volforecast.models.hybrid import HybridResidualForecaster``) so the core
package stays importable without torch.
"""
